/* eslint-disable */

// May contain unused imports in some cases
import { Type } from "./type"

/**
 * 
 * @export
 * @interface Auth
 */
export interface Auth {
    /**
     * 
     * @type {Type}
     * @memberof Auth
     */
    'type': Type;
}
